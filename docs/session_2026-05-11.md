# Session Recap — 2026-05-11

Where we ended the day. Read this first when you come back.

---

## What shipped today (merged to main)

| # | What | Branch (now merged) |
|---|---|---|
| 1 | Verification tools (smoke_test, orphan_check, quality_metrics, compare_extractors) | (direct to main) |
| 2 | Production baseline captured | `outputs/quality_metrics_20260511_103733.json` |
| 3 | PR A — Orphan hardening (race, order, SIGINT) | `fix/orphan-hardening` |
| 4 | PR B — Prompt alignment (build_prompt shared) | `fix/prompt-alignment` |
| 5 | PR C — Safety rails (try-except, eager lookups) | `fix/safety-rails` |
| 6 | PR D — Universal Phase 1 wipe in reset_quality_formatting | `fix/reset-formatting-phase1` |
| 7 | PR F — UTC → ET timestamps + cheat sheet | `fix/utc-to-et-timestamps` |
| 8 | 336 orphans recovered (584 → 248 outstanding) | via events_from_ids.py |
| 9 | Sheet formatting drift cleared | via reset_quality_formatting.py --apply |

## What's pushed but NOT YET merged to main

| Branch | What | Status |
|---|---|---|
| `fix/scratch-sheet-support` | PR H — `--scratch` flag for sandbox testing | Tested by user, ready to merge |
| `fix/tier-escalation` | PR I — 4-tier ladder in main.py + date_sanity hotfix | Tested ONCE, see "Outstanding" below |

## Where the tier-escalation test left off

**Result from scratch run (3 posts, max_workers=1, --scratch):**
- ✓ Scratch mode banner appeared
- ✓ Tier ladder ran end-to-end
- ✓ 121 events extracted across 2 productive posts
- ✓ Final report's new 🎚 TIER USAGE section worked
- Distribution: flash_caption 1 (33%), pro_image 2 (67%)
- ⚠ 8+ `date_sanity` TypeError exceptions caught by _safe_check

**Bug found + fixed (already pushed to `fix/tier-escalation`):**
- `check_date_sanity` in extraction_core was raising TypeError
- Cause: PR F made `datetime.now()` tz-aware; check assumed naive
- Fix: strip tzinfo before subtraction (commit `203b363`)
- This bug is on production main RIGHT NOW. PRs A+C swallow the
  exception silently — events still saved, but PAST_DATE /
  FAR_FUTURE_DATE flags weren't firing.

## To do tomorrow (in order)

### Step 1 — Pull + re-test on scratch
```
git fetch
git checkout fix/tier-escalation
git pull
python smoke_test.py
python main.py --scratch --now
```

This should produce **zero** `date_sanity` warnings now.

### Step 2 — If clean, merge to main IN THIS ORDER
```
git checkout main
git merge fix/scratch-sheet-support    # PR H first (PR I depends on it)
git push
git merge fix/tier-escalation          # then PR I (includes date_sanity fix)
git push
```

### Step 3 — Reset config.json for production cron
Remove the test caps:
```json
{
  "max_posts": 0,          // or delete this line
  "max_workers": 10        // restore from 1
}
```

### Step 4 — Watch Thursday's cron
Expected:
- Tier distribution: most posts at flash_caption, ~5% at pro_image
- Cost drops from ~$2/week → ~$0.20/week
- No `date_sanity` warnings in the run log
- New rows have no hallucinated "New Jersey" cities, no out-of-range confidence

### Step 5 — Post-cron verification (Thursday afternoon)
```
python orphan_check.py
python quality_metrics.py --compare outputs/quality_metrics_20260511_103733.json
```

Watch deltas in `hallucinated_city` and `conf out-of-range`. Both should be flat for old rows + zero new ones.

## Things you might want to do but aren't urgent

- **Cherry-pick the date_sanity hotfix to main NOW** (before PR I) — production main is silently swallowing DATE_SANITY checks every cron. Run:
  ```
  git checkout main
  git cherry-pick 203b363
  git push
  ```
- **Recover the 182 cache-missing orphans** — need a fresh Apify scrape for the date range that produced them. Not urgent.
- **Spot-check 53 NO_GROUNDING + 187 MISSING_DATE flagged rows** in All_Events. Manual review queue.
- **Schema consolidation** (column lists in main.py + events_from_ids.py). I decided it's cosmetic, not needed.

## Tool inventory

See [docs/cheat_sheet.md](cheat_sheet.md) for full reference. Quick recap:

- **Weekly auto-run**: main.py (with tier ladder once PR I lands)
- **Recovery**: events_from_ids.py
- **Cleanup**: audit_regions.py, dedup_all_events.py, reset_quality_formatting.py
- **Diagnostics**: smoke_test.py, orphan_check.py, quality_metrics.py, compare_extractors.py
- **Sandbox**: main.py --scratch (PR H)

## Verification snapshot from end of session

- Baseline (pre-PR-A/B/C/D/F): orphans=584, hallucinated cities=60, conf out-of-range=384
- After orphan recovery: orphans=248 (66 stale, 182 cache-missing)
- After PR D apply: formatting drift cleared
- After tomorrow's cron with PR I merged: expected to see hallucinated cities + conf-out-of-range trending toward 0 on new rows
